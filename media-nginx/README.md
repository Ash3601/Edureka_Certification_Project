# build docker image with nginx configuration

docker build -t ash360/nginx_server .

# to run docker file using docker compose to run all the containers

docker-compose up

# to put down the containers

docker-compose down
